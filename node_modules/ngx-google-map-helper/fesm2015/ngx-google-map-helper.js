import { Injectable, Optional, Component, ElementRef, Input, Output, EventEmitter, NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
 */
class MapConfig {
    constructor() {
        this.apiKey = '';
    }
}

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
 */
class NgxGoogleMapHelperService {
    /**
     * @param {?} config
     */
    constructor(config) {
        this.apiKey = '';
        if (config) {
            this.apiKey = config.apiKey;
        }
    }
    /**
     * @return {?}
     */
    getApiKey() {
        return this.apiKey;
    }
}
NgxGoogleMapHelperService.decorators = [
    { type: Injectable, args: [{
                providedIn: 'root'
            },] },
    { type: Injectable }
];
/** @nocollapse */
NgxGoogleMapHelperService.ctorParameters = () => [
    { type: MapConfig, decorators: [{ type: Optional }] }
];

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
 */
/** @type {?} */
const iconValues = {
    // tslint:disable-next-line:max-line-length
    clear: '<svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" viewBox="0 0 24 24"><path d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"/><path d="M0 0h24v24H0z" fill="none"/></svg>',
    // tslint:disable-next-line:max-line-length
    delete: '<svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" viewBox="0 0 24 24"><path d="M6 19c0 1.1.9 2 2 2h8c1.1 0 2-.9 2-2V7H6v12zM8 9h8v10H8V9zm7.5-5l-1-1h-5l-1 1H5v2h14V4z"/><path fill="none" d="M0 0h24v24H0V0z"/></svg>',
    // tslint:disable-next-line:max-line-length
    save: '<svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" viewBox="0 0 24 24"><path d="M19 12v7H5v-7H3v7c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2v-7h-2zm-6 .67l2.59-2.58L17 11.5l-5 5-5-5 1.41-1.41L11 12.67V3h2z"/><path fill="none" d="M0 0h24v24H0z"/></svg>',
    // tslint:disable-next-line:max-line-length
    saveAll: '<svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" viewBox="0 0 24 24"><path d="M0 0h24v24H0z" fill="none"/><path d="M17 3H5c-1.11 0-2 .9-2 2v14c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V7l-4-4zm-5 16c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3zm3-10H5V5h10v4z"/></svg>',
    // tslint:disable-next-line:max-line-length
    undo: '<svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" viewBox="0 0 24 24"><path d="M0 0h24v24H0z" fill="none"/><path d="M12.5 8c-2.65 0-5.05.99-6.9 2.6L2 7v9h9l-3.62-3.62c1.39-1.16 3.16-1.88 5.12-1.88 3.54 0 6.55 2.31 7.6 5.5l2.37-.78C21.08 11.03 17.15 8 12.5 8z"/></svg>'
};

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
 */
// tslint:disable-next-line:align
// tslint:disable-next-line:one-variable-per-declaration
/** @type {?} */
const defaultValues = {
    drawingModes: ['marker', 'circle', 'polygon', 'polyline', 'rectangle'],
    handlerPositions: ['TOP_CENTER', 'TOP_LEFT', 'TOP_RIGHT', 'BOTTOM_CENTER', 'BOTTOM_LEFT', 'BOTTOM_RIGHT'],
    defaultOverlayValues: {
        fillColor: '#005696',
        fillOpacity: 0.2,
        strokeColor: '#005696',
        strokeWeight: 3,
        clickable: true,
        editable: true,
        draggable: true,
        zIndex: 1
    },
    defaultMarkerValues: {
        animation: 'DROP',
        draggable: true,
        title: '',
        icon: null
    },
    customButtons: [{ key: 'undo', label: 'Undo' }, { key: 'clear', label: 'Clear All' }, { key: 'delete', label: 'Delete Selected' },
        { key: 'save', label: 'Save Selected' }, { key: 'saveAll', label: 'Save All' }],
    height: '500px',
    width: '100%',
    zoom: 10,
    center: { lat: -34.397, lng: 150.644 }
};

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
 */
class NgxGoogleMapHelperComponent {
    /**
     * @param {?} elementRef
     * @param {?} mapService
     */
    constructor(elementRef, mapService) {
        this.elementRef = elementRef;
        // map style inputs
        this.mapHeight = defaultValues.height;
        this.mapWidth = defaultValues.width;
        this.zoom = defaultValues.zoom;
        this.center = null;
        this.mapType = 'ROADMAP';
        // drawing control inputs
        this.placeMarkerOnClick = false;
        this.showControl = true;
        this.position = defaultValues.handlerPositions[0];
        this.showModes = defaultValues.drawingModes;
        this.locationAccess = false;
        this.commonOption = defaultValues.defaultOverlayValues;
        this.markerOption = defaultValues.defaultMarkerValues;
        this.customButtons = true;
        this.onlyCustomButtons = [];
        this.googleMapObjects = false;
        this.showErrors = true;
        this.shapes = [];
        // outputs
        this.mapClicked = new EventEmitter();
        this.overlayCompleted = new EventEmitter();
        this.overlaySelected = new EventEmitter();
        this.saveSeleted = new EventEmitter();
        this.saveAll = new EventEmitter();
        this.isWorldView = false;
        this.allOverlays = [];
        this.customButtonsList = defaultValues.customButtons;
        this.apiKey = mapService.getApiKey();
    }
    /**
     * @return {?}
     */
    ngOnInit() {
        if (this.apiKey && this.apiKey !== '') {
            Promise.all([
                this.lazyLoadMap()
            ]).then((/**
             * @param {?} value
             * @return {?}
             */
            value => this.initMap()));
            this.initOverlayOptions();
        }
        else {
            this.showError('google map key required, include in forroot()', null);
        }
    }
    /**
     * @return {?}
     */
    checkValues() {
        if (!this.mapWidth) {
            this.mapWidth = defaultValues.width;
        }
        if (!this.mapHeight) {
            this.mapHeight = defaultValues.height;
        }
        if (!this.zoom) {
            this.zoom = defaultValues.zoom;
        }
    }
    /**
     * @return {?}
     */
    lazyLoadMap() {
        /** @type {?} */
        const promise = new Promise((/**
         * @param {?} resolve
         * @param {?} reject
         * @return {?}
         */
        (resolve, reject) => {
            if (typeof google === 'object' && typeof google.maps === 'object') {
                resolve();
            }
            else {
                /** @type {?} */
                const s = document.createElement('script');
                s.setAttribute('id', 'googleMap');
                s.type = 'text/javascript';
                s.src = `https://maps.googleapis.com/maps/api/js?key=${this.apiKey}&libraries=drawing`;
                this.elementRef.nativeElement.appendChild(s);
            }
            setTimeout((/**
             * @return {?}
             */
            () => {
                resolve();
            }), 1000);
        }));
        return promise;
    }
    // if the user has overridden the options
    /**
     * @return {?}
     */
    initOverlayOptions() {
        if (!this.circleOption) {
            this.circleOption = this.commonOption;
        }
        if (!this.polygonOption) {
            this.polygonOption = this.commonOption;
        }
        if (!this.polylineOption) {
            this.polylineOption = this.commonOption;
        }
        if (!this.rectangleOption) {
            this.rectangleOption = this.commonOption;
        }
    }
    // initialize the map with location
    /**
     * @return {?}
     */
    initMap() {
        if (this.locationAccess && navigator) {
            navigator.geolocation.getCurrentPosition((/**
             * @param {?} pos
             * @return {?}
             */
            pos => {
                this.center.lng = +pos.coords.longitude;
                this.center.lat = +pos.coords.latitude;
                this.setUpMap(false);
            }), (/**
             * @param {?} error
             * @return {?}
             */
            error => {
                this.setUpMap(true);
                this.isWorldView = true;
                this.showError('error while reading location', null);
            }));
        }
        else {
            this.setUpMap(true);
            this.isWorldView = true;
            if (this.locationAccess) {
                this.showError('please allow location access on browser', null);
            }
        }
    }
    /**
     * @private
     * @return {?}
     */
    setUpWorldView() {
        /** @type {?} */
        const allowedBounds = new google.maps.LatLngBounds(new google.maps.LatLng(85, -180), // top left corner of map
        new google.maps.LatLng(-85, 180) // bottom right corner
        );
        /** @type {?} */
        const k = 5.0;
        /** @type {?} */
        const n = allowedBounds.getNorthEast().lat() - k;
        /** @type {?} */
        const e = allowedBounds.getNorthEast().lng() - k;
        /** @type {?} */
        const s = allowedBounds.getSouthWest().lat() + k;
        /** @type {?} */
        const w = allowedBounds.getSouthWest().lng() + k;
        /** @type {?} */
        const neNew = new google.maps.LatLng(n, e);
        /** @type {?} */
        const swNew = new google.maps.LatLng(s, w);
        /** @type {?} */
        const boundsNew = new google.maps.LatLngBounds(swNew, neNew);
        this.map.fitBounds(boundsNew);
    }
    /**
     * @private
     * @param {?} isWorldView
     * @return {?}
     */
    setUpMap(isWorldView) {
        /** @type {?} */
        let tempCenter;
        if (this.center) {
            tempCenter = this.center;
            this.isWorldView = false;
            isWorldView = false;
        }
        else {
            tempCenter = defaultValues.center;
        }
        this.map = new google.maps.Map(document.getElementById('map'), {
            center: {
                lat: tempCenter.lat,
                lng: tempCenter.lng
            },
            zoom: this.zoom,
            minZoom: 2,
            mapTypeId: google.maps.MapTypeId[this.mapType],
            styles: [
                {
                    featureType: 'administrative.province',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'administrative.locality',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'administrative.neighborhood',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'administrative.land_parcel',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'landscape',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'landscape.man_made',
                    elementType: 'geometry.fill',
                    stylers: [
                        {
                            color: '#e9e5dc'
                        }
                    ]
                },
                {
                    featureType: 'landscape.natural',
                    elementType: 'geometry.fill',
                    stylers: [
                        {
                            visibility: 'on'
                        },
                        {
                            color: '#b8cb93'
                        }
                    ]
                },
                {
                    featureType: 'poi',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'off'
                        }
                    ]
                },
                {
                    featureType: 'poi.attraction',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'poi.business',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'poi.business',
                    elementType: 'geometry.fill',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'poi.government',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'poi.government',
                    elementType: 'geometry.fill',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'poi.medical',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'poi.medical',
                    elementType: 'geometry.fill',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'poi.park',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'poi.park',
                    elementType: 'geometry.fill',
                    stylers: [
                        {
                            color: '#ccdca1'
                        }
                    ]
                },
                {
                    featureType: 'poi.park',
                    elementType: 'labels.text.fill',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'poi.place_of_worship',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'poi.school',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'poi.sports_complex',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'road',
                    elementType: 'all',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'road',
                    elementType: 'geometry.fill',
                    stylers: [
                        {
                            hue: '#ff0000'
                        },
                        {
                            saturation: -100
                        },
                        {
                            lightness: 99
                        }
                    ]
                },
                {
                    featureType: 'road',
                    elementType: 'geometry.stroke',
                    stylers: [
                        {
                            color: '#808080'
                        },
                        {
                            lightness: 54
                        },
                        {
                            visibility: 'off'
                        }
                    ]
                },
                {
                    featureType: 'road',
                    elementType: 'labels.text.fill',
                    stylers: [
                        {
                            color: '#767676'
                        }
                    ]
                },
                {
                    featureType: 'road',
                    elementType: 'labels.text.stroke',
                    stylers: [
                        {
                            color: '#ffffff'
                        }
                    ]
                },
                {
                    featureType: 'transit.station.airport',
                    elementType: 'labels.text.fill',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'transit.station.airport',
                    elementType: 'labels.icon',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                },
                {
                    featureType: 'water',
                    elementType: 'all',
                    stylers: [
                        {
                            saturation: 43
                        },
                        {
                            lightness: -11
                        },
                        {
                            color: '#89cada'
                        }
                    ]
                },
                {
                    featureType: 'water',
                    elementType: 'geometry',
                    stylers: [
                        {
                            visibility: 'on'
                        }
                    ]
                }
            ]
        });
        if (isWorldView) {
            this.setUpWorldView();
        }
        if (this.placeMarkerOnClick) {
            google.maps.event.addListener(this.map, 'click', (/**
             * @param {?} event
             * @return {?}
             */
            function (event) {
                this.placeMarker(event.latLng);
                this.mapClicked.emit(event);
            }).bind(this));
        }
        else {
            google.maps.event.addListener(this.map, 'click', (/**
             * @param {?} event
             * @return {?}
             */
            function (event) {
                this.mapClicked.emit(event);
            }).bind(this));
        }
        this.drawingManager = new google.maps.drawing.DrawingManager({
            drawingMode: google.maps.drawing.OverlayType.MARKER,
            drawingControl: this.showControl,
            drawingControlOptions: {
                // tslint:disable-next-line:no-string-literal
                position: google.maps.ControlPosition[this.position],
                drawingModes: this.showModes
            },
            markerOptions: {
                icon: this.markerOption.icon,
                title: this.markerOption.title,
                animation: google.maps.Animation[this.markerOption.animation],
                draggable: this.markerOption.draggable
            },
            circleOptions: {
                fillColor: this.circleOption.fillColor,
                fillOpacity: this.circleOption.fillOpacity,
                strokeColor: this.circleOption.strokeColor,
                strokeWeight: this.circleOption.strokeWeight,
                clickable: this.circleOption.clickable,
                editable: this.circleOption.editable,
                draggable: this.circleOption.draggable,
                zIndex: this.circleOption.zIndex
            },
            polygonOptions: {
                fillColor: this.polygonOption.fillColor,
                fillOpacity: this.polygonOption.fillOpacity,
                strokeColor: this.polygonOption.strokeColor,
                strokeWeight: this.polygonOption.strokeWeight,
                clickable: this.polygonOption.clickable,
                editable: this.polygonOption.editable,
                draggable: this.polygonOption.draggable,
                zIndex: this.polygonOption.zIndex
            },
            rectangleOptions: {
                fillColor: this.rectangleOption.fillColor,
                fillOpacity: this.rectangleOption.fillOpacity,
                strokeColor: this.rectangleOption.strokeColor,
                strokeWeight: this.rectangleOption.strokeWeight,
                clickable: this.rectangleOption.clickable,
                editable: this.rectangleOption.editable,
                draggable: this.rectangleOption.draggable,
                zIndex: this.rectangleOption.zIndex
            },
            polylineOptions: {
                fillColor: this.polylineOption.fillColor,
                fillOpacity: this.polylineOption.fillOpacity,
                strokeColor: this.polylineOption.strokeColor,
                strokeWeight: this.polylineOption.strokeWeight,
                clickable: this.polylineOption.clickable,
                editable: this.polylineOption.editable,
                draggable: this.polylineOption.draggable,
                zIndex: this.polylineOption.zIndex
            }
        });
        this.drawingManager.setMap(this.map);
        google.maps.event.addListener(this.drawingManager, 'overlaycomplete', (/**
         * @param {?} event
         * @return {?}
         */
        function (event) {
            this.addOverlay(event);
            if (this.googleMapObjects) {
                this.overlayCompleted.emit(event); // output overlay complete
            }
            else {
                /** @type {?} */
                const temp = this.getOverlayType(event);
                this.overlayCompleted.emit(temp);
            }
        }).bind(this));
        if (this.customButtons) {
            this.initCustomButtons(this.map);
        }
        if (this.shapes && this.shapes.length > 0) {
            this.loadShapeToMap(this.shapes);
        }
    }
    /**
     * @private
     * @param {?} event
     * @return {?}
     */
    addOverlay(event) {
        if (this.allOverlays) {
            this.allOverlays.push(event);
        }
        if (event.type !== google.maps.drawing.OverlayType.MARKER) {
            this.drawingManager.setDrawingMode(null);
            /** @type {?} */
            const newShape = event.overlay;
            newShape.type = event.type;
            google.maps.event.addListener(newShape, 'click', (/**
             * @return {?}
             */
            function () {
                this.setSelection(newShape);
            }).bind(this));
            this.setSelection(newShape);
        }
    }
    // Place a marker on click
    /**
     * @param {?} location
     * @return {?}
     */
    placeMarker(location) {
        /** @type {?} */
        const marker = new google.maps.Marker({
            position: location,
            map: this.map,
            icon: this.markerOption.icon,
            title: this.markerOption.title,
            animation: google.maps.Animation[this.markerOption.animation],
            draggable: this.markerOption.draggable
        });
        /** @type {?} */
        const overlay = {
            type: 'marker',
            overlay: marker
        };
        this.addOverlay(overlay);
    }
    // Initialize custom buttons
    /**
     * @param {?} map
     * @return {?}
     */
    initCustomButtons(map) {
        /** @type {?} */
        let showButtons = this.customButtonsList;
        if (this.onlyCustomButtons && this.onlyCustomButtons.length > 0) {
            showButtons = this.customButtonsList.filter((/**
             * @param {?} a
             * @return {?}
             */
            a => this.onlyCustomButtons.includes(a.key)));
        }
        if (showButtons && showButtons.length > 0) {
            for (const button of showButtons) {
                /** @type {?} */
                const controlDiv = document.createElement('div');
                controlDiv.style.marginRight = '2px';
                /** @type {?} */
                const controlUI = document.createElement('div');
                controlUI.style.backgroundColor = '#fff';
                controlUI.style.border = '2px solid #fff';
                controlUI.style.borderRadius = '3px';
                controlUI.style.boxShadow = '0 2px 6px rgba(0,0,0,.3)';
                controlUI.style.cursor = 'pointer';
                controlUI.style.marginTop = '5px';
                controlUI.style.marginBottom = '5px';
                controlUI.style.width = '23px';
                controlUI.style.height = '20px';
                controlUI.style.textAlign = 'center';
                controlUI.title = button.label;
                controlDiv.appendChild(controlUI);
                /** @type {?} */
                const controlText = document.createElement('div');
                controlText.style.color = 'rgb(25,25,25)';
                controlText.style.fontFamily = 'Roboto,Arial,sans-serif';
                controlText.style.fontSize = '12px';
                controlText.style.lineHeight = '20px';
                controlText.style.paddingLeft = '3px';
                controlText.style.paddingRight = '3px';
                controlText.innerHTML = `<span> ${iconValues[button.key]} </span>`;
                controlUI.appendChild(controlText);
                controlUI.addEventListener('click', (/**
                 * @return {?}
                 */
                function () {
                    this.handleCustomButtonEvent(button.key);
                }).bind(this));
                map.controls[google.maps.ControlPosition[this.position]].push(controlDiv);
            }
        }
        else {
            this.showError('please check onlyCustomButtons property', this.onlyCustomButtons);
        }
    }
    // Handles custom button events
    /**
     * @param {?} button
     * @return {?}
     */
    handleCustomButtonEvent(button) {
        switch (button) {
            case 'undo': {
                this.undoShape();
                break;
            }
            case 'clear': {
                this.deleteAllShape();
                break;
            }
            case 'delete': {
                this.deleteSelectedShape();
                break;
            }
            case 'save': {
                this.saveSelectedShape();
                break;
            }
            case 'saveAll': {
                this.saveAllShape();
                break;
            }
        }
    }
    // set selected shape on map
    /**
     * @param {?} shape
     * @return {?}
     */
    setSelection(shape) {
        this.clearSelection();
        this.selectedShape = shape;
        shape.setEditable(true);
        if (this.googleMapObjects) {
            this.overlaySelected.emit(shape); // output overlay selected
        }
        else {
            /** @type {?} */
            const temp = this.getOverlayType(shape);
            this.overlaySelected.emit(temp);
        }
    }
    // clears selected shape on map
    /**
     * @return {?}
     */
    clearSelection() {
        if (this.selectedShape) {
            this.selectedShape.setEditable(false);
            this.selectedShape = null;
        }
    }
    // deletes selected shape
    /**
     * @return {?}
     */
    deleteSelectedShape() {
        if (this.selectedShape) {
            this.selectedShape.setMap(null);
        }
    }
    // outputs the selected shape
    /**
     * @return {?}
     */
    saveSelectedShape() {
        if (this.googleMapObjects) {
            this.saveSeleted.emit(this.selectedShape); // output overlay selected
        }
        else {
            if (this.selectedShape) {
                /** @type {?} */
                const shape = this.getOverlayType(this.selectedShape);
                this.saveSeleted.emit(shape);
            }
        }
    }
    // deletes all the shape on map
    /**
     * @return {?}
     */
    deleteAllShape() {
        // tslint:disable-next-line:prefer-for-of
        for (let i = 0; i < this.allOverlays.length; i++) {
            this.allOverlays[i].overlay.setMap(null);
        }
        this.allOverlays = [];
    }
    // undo the last action
    /**
     * @return {?}
     */
    undoShape() {
        if (this.allOverlays !== undefined && this.allOverlays !== null && this.allOverlays.length >= 1) {
            this.allOverlays[this.allOverlays.length - 1].overlay.setMap(null);
            this.allOverlays.pop();
        }
    }
    // outputs all shapes
    /**
     * @return {?}
     */
    saveAllShape() {
        if (this.allOverlays && this.allOverlays.length > 0) {
            if (this.googleMapObjects) {
                this.saveAll.emit(this.allOverlays);
            }
            else {
                /** @type {?} */
                const shapes = [];
                for (const overLay of this.allOverlays) {
                    if (overLay) {
                        /** @type {?} */
                        const shape = this.getOverlayType(overLay);
                        if (shape) {
                            shapes.push(shape);
                        }
                    }
                }
                this.saveAll.emit(shapes);
            }
        }
    }
    // returns an shape object
    /**
     * @private
     * @param {?} object
     * @return {?}
     */
    getOverlayType(object) {
        /** @type {?} */
        let shape = null;
        /** @type {?} */
        let overlay = null;
        if (object.map) {
            overlay = object;
        }
        else if (object.overlay.map) {
            overlay = object.overlay;
        }
        else {
            this.showError('Error while reading overlay, try googleMapObjects = true', object);
            return;
        }
        if (object.type === 'circle') {
            /** @type {?} */
            const center = { lat: overlay.center.lat(), lng: overlay.center.lng() };
            /** @type {?} */
            const circle = { center, radius: overlay.radius };
            shape = {
                type: object.type,
                shape: circle
            };
        }
        else if (object.type === 'polygon' || object.type === 'polyline') {
            /** @type {?} */
            const center = { lat: overlay.map.center.lat(), lng: overlay.map.center.lng() };
            /** @type {?} */
            const tempPoints = overlay.latLngs.j[0].j;
            /** @type {?} */
            const points = [];
            for (const p of tempPoints) {
                /** @type {?} */
                const point = { lat: p.lat(), lng: p.lng() };
                points.push(point);
            }
            /** @type {?} */
            const polygon = { center, points };
            shape = {
                type: object.type,
                shape: polygon
            };
        }
        else if (object.type === 'rectangle') {
            /** @type {?} */
            const center = { lat: overlay.map.center.lat(), lng: overlay.map.center.lng() };
            // tslint:disable-next-line:no-shadowed-variable
            /** @type {?} */
            const bounds = overlay.getBounds();
            /** @type {?} */
            const neBound = { lat: bounds.getNorthEast().lat(), lng: bounds.getNorthEast().lng() };
            /** @type {?} */
            const swBound = { lat: bounds.getSouthWest().lat(), lng: bounds.getSouthWest().lng() };
            /** @type {?} */
            const rectangle = { center, northEastBound: neBound, southWestBound: swBound };
            shape = {
                type: object.type,
                shape: rectangle
            };
        }
        else if (object.type === 'marker') {
            /** @type {?} */
            const center = { lat: overlay.map.center.lat(), lng: overlay.map.center.lng() };
            /** @type {?} */
            const position = { lat: overlay.position.lat(), lng: overlay.position.lng() };
            /** @type {?} */
            const marker = { center, position };
            shape = {
                type: object.type,
                shape: marker
            };
        }
        return shape;
    }
    // sets shapes on map
    /**
     * @private
     * @param {?} shapes
     * @return {?}
     */
    loadShapeToMap(shapes) {
        if (shapes && shapes.length > 0) {
            for (let i = 0; i < shapes.length; i++) {
                /** @type {?} */
                const shape = shapes[i];
                if (shape.type === 'marker') {
                    /** @type {?} */
                    const markerObj = shape.shape;
                    /** @type {?} */
                    const marker = new google.maps.Marker({
                        position: markerObj.position,
                        map: this.map,
                        icon: this.markerOption.icon,
                        title: this.markerOption.title,
                        animation: google.maps.Animation[this.markerOption.animation],
                        draggable: this.markerOption.draggable
                    });
                    /** @type {?} */
                    const overlay = {
                        type: 'marker',
                        overlay: marker
                    };
                    this.addOverlay(overlay);
                    if (this.isWorldView && i === 0) {
                        /** @type {?} */
                        const bounds = new google.maps.LatLngBounds();
                        /** @type {?} */
                        const latLng = new google.maps.LatLng(markerObj.position.lat, markerObj.position.lng);
                        bounds.extend(latLng);
                        this.map.fitBounds(bounds);
                    }
                }
                else if (shape.type === 'circle') {
                    /** @type {?} */
                    const circleObj = shape.shape;
                    /** @type {?} */
                    const circle = new google.maps.Circle({
                        fillColor: this.circleOption.fillColor,
                        fillOpacity: this.circleOption.fillOpacity,
                        strokeColor: this.circleOption.strokeColor,
                        strokeWeight: this.circleOption.strokeWeight,
                        clickable: this.circleOption.clickable,
                        editable: this.circleOption.editable,
                        draggable: this.circleOption.draggable,
                        zIndex: this.circleOption.zIndex,
                        map: this.map,
                        center: circleObj.center,
                        radius: circleObj.radius
                    });
                    /** @type {?} */
                    const overlay = {
                        type: 'circle',
                        overlay: circle
                    };
                    this.addOverlay(overlay);
                    if (this.isWorldView && i === 0) {
                        this.map.fitBounds(circle.getBounds());
                    }
                }
                else if (shape.type === 'polygon') {
                    /** @type {?} */
                    const polygonObj = shape.shape;
                    /** @type {?} */
                    const polygon = new google.maps.Polygon({
                        paths: polygonObj.points,
                        fillColor: this.polygonOption.fillColor,
                        fillOpacity: this.polygonOption.fillOpacity,
                        strokeColor: this.polygonOption.strokeColor,
                        strokeWeight: this.polygonOption.strokeWeight,
                        clickable: this.polygonOption.clickable,
                        editable: this.polygonOption.editable,
                        draggable: this.polygonOption.draggable,
                        zIndex: this.polygonOption.zIndex
                    });
                    polygon.setMap(this.map);
                    /** @type {?} */
                    const overlay = {
                        type: 'polygon',
                        overlay: polygon
                    };
                    this.addOverlay(overlay);
                    if (this.isWorldView && i === 0) {
                        /** @type {?} */
                        const bounds = new google.maps.LatLngBounds();
                        // tslint:disable-next-line:prefer-for-of
                        for (let j = 0; j < polygonObj.points.length; j++) {
                            /** @type {?} */
                            const latLng = new google.maps.LatLng(polygonObj.points[j].lat, polygonObj.points[j].lng);
                            bounds.extend(latLng);
                        }
                        this.map.fitBounds(bounds);
                    }
                }
                else if (shape.type === 'rectangle') {
                    /** @type {?} */
                    const rectangleObj = shape.shape;
                    /** @type {?} */
                    const rectangle = new google.maps.Rectangle({
                        fillColor: this.rectangleOption.fillColor,
                        fillOpacity: this.rectangleOption.fillOpacity,
                        strokeColor: this.rectangleOption.strokeColor,
                        strokeWeight: this.rectangleOption.strokeWeight,
                        clickable: this.rectangleOption.clickable,
                        editable: this.rectangleOption.editable,
                        draggable: this.rectangleOption.draggable,
                        zIndex: this.rectangleOption.zIndex,
                        map: this.map,
                        bounds: {
                            north: rectangleObj.northEastBound.lat,
                            south: rectangleObj.southWestBound.lat,
                            east: rectangleObj.northEastBound.lng,
                            west: rectangleObj.southWestBound.lng
                        }
                    });
                    /** @type {?} */
                    const overlay = {
                        type: 'rectangle',
                        overlay: rectangle
                    };
                    this.addOverlay(overlay);
                    if (this.isWorldView && i === 0) {
                        this.map.fitBounds(rectangle.getBounds());
                    }
                }
                else if (shape.type === 'polyline') {
                    /** @type {?} */
                    const polygonObj = shape.shape;
                    /** @type {?} */
                    const polygon = new google.maps.Polygon({
                        paths: polygonObj.points,
                        fillColor: this.polygonOption.fillColor,
                        fillOpacity: this.polygonOption.fillOpacity,
                        strokeColor: this.polygonOption.strokeColor,
                        strokeWeight: this.polygonOption.strokeWeight,
                        clickable: this.polygonOption.clickable,
                        editable: this.polygonOption.editable,
                        draggable: this.polygonOption.draggable,
                        zIndex: this.polygonOption.zIndex
                    });
                    polygon.setMap(this.map);
                    /** @type {?} */
                    const overlay = {
                        type: 'polyline',
                        overlay: polygon
                    };
                    this.addOverlay(overlay);
                    if (this.isWorldView && i === 0) {
                        /** @type {?} */
                        const bounds = new google.maps.LatLngBounds();
                        // tslint:disable-next-line:prefer-for-of
                        for (let j = 0; j < polygonObj.points.length; j++) {
                            /** @type {?} */
                            const latLng = new google.maps.LatLng(polygonObj.points[j].lat, polygonObj.points[j].lng);
                            bounds.extend(latLng);
                        }
                        this.map.fitBounds(bounds);
                    }
                }
            }
        }
    }
    /**
     * @private
     * @param {?} message
     * @param {?} object
     * @return {?}
     */
    showError(message, object) {
        if (this.showErrors) {
            if (object) {
                console.error(`ngx-google-map-helper: ${message}`, object);
            }
            else {
                console.error(`ngx-google-map-helper: ${message}`);
            }
        }
    }
}
NgxGoogleMapHelperComponent.decorators = [
    { type: Component, args: [{
                // tslint:disable-next-line:component-selector
                selector: 'ngx-google-map-helper',
                template: "\r\n<div class=\"map-wrapper\" [ngStyle]=\"{'width': mapWidth, 'height': mapHeight}\">\r\n  <div id=\"map\" class=\"google-map\" style=\"height:100%; margin: 0\"></div>\r\n</div>\r\n\r\n  ",
                styles: [".map-wrapper{position:relative;left:0;bottom:0}"]
            }] }
];
/** @nocollapse */
NgxGoogleMapHelperComponent.ctorParameters = () => [
    { type: ElementRef },
    { type: NgxGoogleMapHelperService }
];
NgxGoogleMapHelperComponent.propDecorators = {
    mapHeight: [{ type: Input }],
    mapWidth: [{ type: Input }],
    zoom: [{ type: Input }],
    center: [{ type: Input }],
    mapType: [{ type: Input }],
    placeMarkerOnClick: [{ type: Input }],
    showControl: [{ type: Input }],
    position: [{ type: Input }],
    showModes: [{ type: Input }],
    locationAccess: [{ type: Input }],
    commonOption: [{ type: Input }],
    circleOption: [{ type: Input }],
    polygonOption: [{ type: Input }],
    polylineOption: [{ type: Input }],
    rectangleOption: [{ type: Input }],
    markerOption: [{ type: Input }],
    customButtons: [{ type: Input }],
    onlyCustomButtons: [{ type: Input }],
    googleMapObjects: [{ type: Input }],
    showErrors: [{ type: Input }],
    shapes: [{ type: Input }],
    mapClicked: [{ type: Output }],
    overlayCompleted: [{ type: Output }],
    overlaySelected: [{ type: Output }],
    saveSeleted: [{ type: Output }],
    saveAll: [{ type: Output }]
};

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
 */
class NgxGoogleMapHelperModule {
    /**
     * @param {?} config
     * @return {?}
     */
    static forRoot(config) {
        return {
            ngModule: NgxGoogleMapHelperModule,
            providers: [
                { provide: MapConfig, useValue: config }
            ]
        };
    }
}
NgxGoogleMapHelperModule.decorators = [
    { type: NgModule, args: [{
                imports: [CommonModule],
                declarations: [NgxGoogleMapHelperComponent],
                exports: [NgxGoogleMapHelperComponent],
                providers: [NgxGoogleMapHelperService]
            },] }
];

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
 */

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
 */

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
 */

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
 */

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
 */

export { NgxGoogleMapHelperService, NgxGoogleMapHelperComponent, NgxGoogleMapHelperModule, MapConfig as ɵa };

//# sourceMappingURL=ngx-google-map-helper.js.map