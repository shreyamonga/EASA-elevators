(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports, require('@angular/core'), require('@angular/common')) :
    typeof define === 'function' && define.amd ? define('ngx-google-map-helper', ['exports', '@angular/core', '@angular/common'], factory) :
    (factory((global['ngx-google-map-helper'] = {}),global.ng.core,global.ng.common));
}(this, (function (exports,core,common) { 'use strict';

    /**
     * @fileoverview added by tsickle
     * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
     */
    var MapConfig = /** @class */ (function () {
        function MapConfig() {
            this.apiKey = '';
        }
        return MapConfig;
    }());

    /**
     * @fileoverview added by tsickle
     * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
     */
    var NgxGoogleMapHelperService = /** @class */ (function () {
        function NgxGoogleMapHelperService(config) {
            this.apiKey = '';
            if (config) {
                this.apiKey = config.apiKey;
            }
        }
        /**
         * @return {?}
         */
        NgxGoogleMapHelperService.prototype.getApiKey = /**
         * @return {?}
         */
            function () {
                return this.apiKey;
            };
        NgxGoogleMapHelperService.decorators = [
            { type: core.Injectable, args: [{
                        providedIn: 'root'
                    },] },
            { type: core.Injectable }
        ];
        /** @nocollapse */
        NgxGoogleMapHelperService.ctorParameters = function () {
            return [
                { type: MapConfig, decorators: [{ type: core.Optional }] }
            ];
        };
        return NgxGoogleMapHelperService;
    }());

    /*! *****************************************************************************
    Copyright (c) Microsoft Corporation. All rights reserved.
    Licensed under the Apache License, Version 2.0 (the "License"); you may not use
    this file except in compliance with the License. You may obtain a copy of the
    License at http://www.apache.org/licenses/LICENSE-2.0

    THIS CODE IS PROVIDED ON AN *AS IS* BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
    KIND, EITHER EXPRESS OR IMPLIED, INCLUDING WITHOUT LIMITATION ANY IMPLIED
    WARRANTIES OR CONDITIONS OF TITLE, FITNESS FOR A PARTICULAR PURPOSE,
    MERCHANTABLITY OR NON-INFRINGEMENT.

    See the Apache Version 2.0 License for specific language governing permissions
    and limitations under the License.
    ***************************************************************************** */
    function __values(o) {
        var m = typeof Symbol === "function" && o[Symbol.iterator], i = 0;
        if (m)
            return m.call(o);
        return {
            next: function () {
                if (o && i >= o.length)
                    o = void 0;
                return { value: o && o[i++], done: !o };
            }
        };
    }

    /**
     * @fileoverview added by tsickle
     * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
     */
    /** @type {?} */
    var iconValues = {
        // tslint:disable-next-line:max-line-length
        clear: '<svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" viewBox="0 0 24 24"><path d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"/><path d="M0 0h24v24H0z" fill="none"/></svg>',
        // tslint:disable-next-line:max-line-length
        delete: '<svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" viewBox="0 0 24 24"><path d="M6 19c0 1.1.9 2 2 2h8c1.1 0 2-.9 2-2V7H6v12zM8 9h8v10H8V9zm7.5-5l-1-1h-5l-1 1H5v2h14V4z"/><path fill="none" d="M0 0h24v24H0V0z"/></svg>',
        // tslint:disable-next-line:max-line-length
        save: '<svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" viewBox="0 0 24 24"><path d="M19 12v7H5v-7H3v7c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2v-7h-2zm-6 .67l2.59-2.58L17 11.5l-5 5-5-5 1.41-1.41L11 12.67V3h2z"/><path fill="none" d="M0 0h24v24H0z"/></svg>',
        // tslint:disable-next-line:max-line-length
        saveAll: '<svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" viewBox="0 0 24 24"><path d="M0 0h24v24H0z" fill="none"/><path d="M17 3H5c-1.11 0-2 .9-2 2v14c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V7l-4-4zm-5 16c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3zm3-10H5V5h10v4z"/></svg>',
        // tslint:disable-next-line:max-line-length
        undo: '<svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" viewBox="0 0 24 24"><path d="M0 0h24v24H0z" fill="none"/><path d="M12.5 8c-2.65 0-5.05.99-6.9 2.6L2 7v9h9l-3.62-3.62c1.39-1.16 3.16-1.88 5.12-1.88 3.54 0 6.55 2.31 7.6 5.5l2.37-.78C21.08 11.03 17.15 8 12.5 8z"/></svg>'
    };

    /**
     * @fileoverview added by tsickle
     * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
     */
    // tslint:disable-next-line:align
    // tslint:disable-next-line:one-variable-per-declaration
    /** @type {?} */
    var defaultValues = {
        drawingModes: ['marker', 'circle', 'polygon', 'polyline', 'rectangle'],
        handlerPositions: ['TOP_CENTER', 'TOP_LEFT', 'TOP_RIGHT', 'BOTTOM_CENTER', 'BOTTOM_LEFT', 'BOTTOM_RIGHT'],
        defaultOverlayValues: {
            fillColor: '#005696',
            fillOpacity: 0.2,
            strokeColor: '#005696',
            strokeWeight: 3,
            clickable: true,
            editable: true,
            draggable: true,
            zIndex: 1
        },
        defaultMarkerValues: {
            animation: 'DROP',
            draggable: true,
            title: '',
            icon: null
        },
        customButtons: [{ key: 'undo', label: 'Undo' }, { key: 'clear', label: 'Clear All' }, { key: 'delete', label: 'Delete Selected' },
            { key: 'save', label: 'Save Selected' }, { key: 'saveAll', label: 'Save All' }],
        height: '500px',
        width: '100%',
        zoom: 10,
        center: { lat: -34.397, lng: 150.644 }
    };

    /**
     * @fileoverview added by tsickle
     * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
     */
    var NgxGoogleMapHelperComponent = /** @class */ (function () {
        function NgxGoogleMapHelperComponent(elementRef, mapService) {
            this.elementRef = elementRef;
            // map style inputs
            this.mapHeight = defaultValues.height;
            this.mapWidth = defaultValues.width;
            this.zoom = defaultValues.zoom;
            this.center = null;
            this.mapType = 'ROADMAP';
            // drawing control inputs
            this.placeMarkerOnClick = false;
            this.showControl = true;
            this.position = defaultValues.handlerPositions[0];
            this.showModes = defaultValues.drawingModes;
            this.locationAccess = false;
            this.commonOption = defaultValues.defaultOverlayValues;
            this.markerOption = defaultValues.defaultMarkerValues;
            this.customButtons = true;
            this.onlyCustomButtons = [];
            this.googleMapObjects = false;
            this.showErrors = true;
            this.shapes = [];
            // outputs
            this.mapClicked = new core.EventEmitter();
            this.overlayCompleted = new core.EventEmitter();
            this.overlaySelected = new core.EventEmitter();
            this.saveSeleted = new core.EventEmitter();
            this.saveAll = new core.EventEmitter();
            this.isWorldView = false;
            this.allOverlays = [];
            this.customButtonsList = defaultValues.customButtons;
            this.apiKey = mapService.getApiKey();
        }
        /**
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.ngOnInit = /**
         * @return {?}
         */
            function () {
                var _this = this;
                if (this.apiKey && this.apiKey !== '') {
                    Promise.all([
                        this.lazyLoadMap()
                    ]).then(( /**
                     * @param {?} value
                     * @return {?}
                     */function (value) { return _this.initMap(); }));
                    this.initOverlayOptions();
                }
                else {
                    this.showError('google map key required, include in forroot()', null);
                }
            };
        /**
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.checkValues = /**
         * @return {?}
         */
            function () {
                if (!this.mapWidth) {
                    this.mapWidth = defaultValues.width;
                }
                if (!this.mapHeight) {
                    this.mapHeight = defaultValues.height;
                }
                if (!this.zoom) {
                    this.zoom = defaultValues.zoom;
                }
            };
        /**
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.lazyLoadMap = /**
         * @return {?}
         */
            function () {
                var _this = this;
                /** @type {?} */
                var promise = new Promise(( /**
                 * @param {?} resolve
                 * @param {?} reject
                 * @return {?}
                 */function (resolve, reject) {
                    if (typeof google === 'object' && typeof google.maps === 'object') {
                        resolve();
                    }
                    else {
                        /** @type {?} */
                        var s = document.createElement('script');
                        s.setAttribute('id', 'googleMap');
                        s.type = 'text/javascript';
                        s.src = "https://maps.googleapis.com/maps/api/js?key=" + _this.apiKey + "&libraries=drawing";
                        _this.elementRef.nativeElement.appendChild(s);
                    }
                    setTimeout(( /**
                     * @return {?}
                     */function () {
                        resolve();
                    }), 1000);
                }));
                return promise;
            };
        // if the user has overridden the options
        // if the user has overridden the options
        /**
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.initOverlayOptions =
            // if the user has overridden the options
            /**
             * @return {?}
             */
            function () {
                if (!this.circleOption) {
                    this.circleOption = this.commonOption;
                }
                if (!this.polygonOption) {
                    this.polygonOption = this.commonOption;
                }
                if (!this.polylineOption) {
                    this.polylineOption = this.commonOption;
                }
                if (!this.rectangleOption) {
                    this.rectangleOption = this.commonOption;
                }
            };
        // initialize the map with location
        // initialize the map with location
        /**
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.initMap =
            // initialize the map with location
            /**
             * @return {?}
             */
            function () {
                var _this = this;
                if (this.locationAccess && navigator) {
                    navigator.geolocation.getCurrentPosition(( /**
                     * @param {?} pos
                     * @return {?}
                     */function (pos) {
                        _this.center.lng = +pos.coords.longitude;
                        _this.center.lat = +pos.coords.latitude;
                        _this.setUpMap(false);
                    }), ( /**
                     * @param {?} error
                     * @return {?}
                     */function (error) {
                        _this.setUpMap(true);
                        _this.isWorldView = true;
                        _this.showError('error while reading location', null);
                    }));
                }
                else {
                    this.setUpMap(true);
                    this.isWorldView = true;
                    if (this.locationAccess) {
                        this.showError('please allow location access on browser', null);
                    }
                }
            };
        /**
         * @private
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.setUpWorldView = /**
         * @private
         * @return {?}
         */
            function () {
                /** @type {?} */
                var allowedBounds = new google.maps.LatLngBounds(new google.maps.LatLng(85, -180), // top left corner of map
                new google.maps.LatLng(-85, 180) // bottom right corner
                );
                /** @type {?} */
                var k = 5.0;
                /** @type {?} */
                var n = allowedBounds.getNorthEast().lat() - k;
                /** @type {?} */
                var e = allowedBounds.getNorthEast().lng() - k;
                /** @type {?} */
                var s = allowedBounds.getSouthWest().lat() + k;
                /** @type {?} */
                var w = allowedBounds.getSouthWest().lng() + k;
                /** @type {?} */
                var neNew = new google.maps.LatLng(n, e);
                /** @type {?} */
                var swNew = new google.maps.LatLng(s, w);
                /** @type {?} */
                var boundsNew = new google.maps.LatLngBounds(swNew, neNew);
                this.map.fitBounds(boundsNew);
            };
        /**
         * @private
         * @param {?} isWorldView
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.setUpMap = /**
         * @private
         * @param {?} isWorldView
         * @return {?}
         */
            function (isWorldView) {
                /** @type {?} */
                var tempCenter;
                if (this.center) {
                    tempCenter = this.center;
                    this.isWorldView = false;
                    isWorldView = false;
                }
                else {
                    tempCenter = defaultValues.center;
                }
                this.map = new google.maps.Map(document.getElementById('map'), {
                    center: {
                        lat: tempCenter.lat,
                        lng: tempCenter.lng
                    },
                    zoom: this.zoom,
                    minZoom: 2,
                    mapTypeId: google.maps.MapTypeId[this.mapType],
                    styles: [
                        {
                            featureType: 'administrative.province',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'administrative.locality',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'administrative.neighborhood',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'administrative.land_parcel',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'landscape',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'landscape.man_made',
                            elementType: 'geometry.fill',
                            stylers: [
                                {
                                    color: '#e9e5dc'
                                }
                            ]
                        },
                        {
                            featureType: 'landscape.natural',
                            elementType: 'geometry.fill',
                            stylers: [
                                {
                                    visibility: 'on'
                                },
                                {
                                    color: '#b8cb93'
                                }
                            ]
                        },
                        {
                            featureType: 'poi',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'off'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.attraction',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.business',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.business',
                            elementType: 'geometry.fill',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.government',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.government',
                            elementType: 'geometry.fill',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.medical',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.medical',
                            elementType: 'geometry.fill',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.park',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.park',
                            elementType: 'geometry.fill',
                            stylers: [
                                {
                                    color: '#ccdca1'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.park',
                            elementType: 'labels.text.fill',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.place_of_worship',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.school',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'poi.sports_complex',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'road',
                            elementType: 'all',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'road',
                            elementType: 'geometry.fill',
                            stylers: [
                                {
                                    hue: '#ff0000'
                                },
                                {
                                    saturation: -100
                                },
                                {
                                    lightness: 99
                                }
                            ]
                        },
                        {
                            featureType: 'road',
                            elementType: 'geometry.stroke',
                            stylers: [
                                {
                                    color: '#808080'
                                },
                                {
                                    lightness: 54
                                },
                                {
                                    visibility: 'off'
                                }
                            ]
                        },
                        {
                            featureType: 'road',
                            elementType: 'labels.text.fill',
                            stylers: [
                                {
                                    color: '#767676'
                                }
                            ]
                        },
                        {
                            featureType: 'road',
                            elementType: 'labels.text.stroke',
                            stylers: [
                                {
                                    color: '#ffffff'
                                }
                            ]
                        },
                        {
                            featureType: 'transit.station.airport',
                            elementType: 'labels.text.fill',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'transit.station.airport',
                            elementType: 'labels.icon',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        },
                        {
                            featureType: 'water',
                            elementType: 'all',
                            stylers: [
                                {
                                    saturation: 43
                                },
                                {
                                    lightness: -11
                                },
                                {
                                    color: '#89cada'
                                }
                            ]
                        },
                        {
                            featureType: 'water',
                            elementType: 'geometry',
                            stylers: [
                                {
                                    visibility: 'on'
                                }
                            ]
                        }
                    ]
                });
                if (isWorldView) {
                    this.setUpWorldView();
                }
                if (this.placeMarkerOnClick) {
                    google.maps.event.addListener(this.map, 'click', ( /**
                     * @param {?} event
                     * @return {?}
                     */function (event) {
                        this.placeMarker(event.latLng);
                        this.mapClicked.emit(event);
                    }).bind(this));
                }
                else {
                    google.maps.event.addListener(this.map, 'click', ( /**
                     * @param {?} event
                     * @return {?}
                     */function (event) {
                        this.mapClicked.emit(event);
                    }).bind(this));
                }
                this.drawingManager = new google.maps.drawing.DrawingManager({
                    drawingMode: google.maps.drawing.OverlayType.MARKER,
                    drawingControl: this.showControl,
                    drawingControlOptions: {
                        // tslint:disable-next-line:no-string-literal
                        position: google.maps.ControlPosition[this.position],
                        drawingModes: this.showModes
                    },
                    markerOptions: {
                        icon: this.markerOption.icon,
                        title: this.markerOption.title,
                        animation: google.maps.Animation[this.markerOption.animation],
                        draggable: this.markerOption.draggable
                    },
                    circleOptions: {
                        fillColor: this.circleOption.fillColor,
                        fillOpacity: this.circleOption.fillOpacity,
                        strokeColor: this.circleOption.strokeColor,
                        strokeWeight: this.circleOption.strokeWeight,
                        clickable: this.circleOption.clickable,
                        editable: this.circleOption.editable,
                        draggable: this.circleOption.draggable,
                        zIndex: this.circleOption.zIndex
                    },
                    polygonOptions: {
                        fillColor: this.polygonOption.fillColor,
                        fillOpacity: this.polygonOption.fillOpacity,
                        strokeColor: this.polygonOption.strokeColor,
                        strokeWeight: this.polygonOption.strokeWeight,
                        clickable: this.polygonOption.clickable,
                        editable: this.polygonOption.editable,
                        draggable: this.polygonOption.draggable,
                        zIndex: this.polygonOption.zIndex
                    },
                    rectangleOptions: {
                        fillColor: this.rectangleOption.fillColor,
                        fillOpacity: this.rectangleOption.fillOpacity,
                        strokeColor: this.rectangleOption.strokeColor,
                        strokeWeight: this.rectangleOption.strokeWeight,
                        clickable: this.rectangleOption.clickable,
                        editable: this.rectangleOption.editable,
                        draggable: this.rectangleOption.draggable,
                        zIndex: this.rectangleOption.zIndex
                    },
                    polylineOptions: {
                        fillColor: this.polylineOption.fillColor,
                        fillOpacity: this.polylineOption.fillOpacity,
                        strokeColor: this.polylineOption.strokeColor,
                        strokeWeight: this.polylineOption.strokeWeight,
                        clickable: this.polylineOption.clickable,
                        editable: this.polylineOption.editable,
                        draggable: this.polylineOption.draggable,
                        zIndex: this.polylineOption.zIndex
                    }
                });
                this.drawingManager.setMap(this.map);
                google.maps.event.addListener(this.drawingManager, 'overlaycomplete', ( /**
                 * @param {?} event
                 * @return {?}
                 */function (event) {
                    this.addOverlay(event);
                    if (this.googleMapObjects) {
                        this.overlayCompleted.emit(event); // output overlay complete
                    }
                    else {
                        /** @type {?} */
                        var temp = this.getOverlayType(event);
                        this.overlayCompleted.emit(temp);
                    }
                }).bind(this));
                if (this.customButtons) {
                    this.initCustomButtons(this.map);
                }
                if (this.shapes && this.shapes.length > 0) {
                    this.loadShapeToMap(this.shapes);
                }
            };
        /**
         * @private
         * @param {?} event
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.addOverlay = /**
         * @private
         * @param {?} event
         * @return {?}
         */
            function (event) {
                if (this.allOverlays) {
                    this.allOverlays.push(event);
                }
                if (event.type !== google.maps.drawing.OverlayType.MARKER) {
                    this.drawingManager.setDrawingMode(null);
                    /** @type {?} */
                    var newShape_1 = event.overlay;
                    newShape_1.type = event.type;
                    google.maps.event.addListener(newShape_1, 'click', ( /**
                     * @return {?}
                     */function () {
                        this.setSelection(newShape_1);
                    }).bind(this));
                    this.setSelection(newShape_1);
                }
            };
        // Place a marker on click
        // Place a marker on click
        /**
         * @param {?} location
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.placeMarker =
            // Place a marker on click
            /**
             * @param {?} location
             * @return {?}
             */
            function (location) {
                /** @type {?} */
                var marker = new google.maps.Marker({
                    position: location,
                    map: this.map,
                    icon: this.markerOption.icon,
                    title: this.markerOption.title,
                    animation: google.maps.Animation[this.markerOption.animation],
                    draggable: this.markerOption.draggable
                });
                /** @type {?} */
                var overlay = {
                    type: 'marker',
                    overlay: marker
                };
                this.addOverlay(overlay);
            };
        // Initialize custom buttons
        // Initialize custom buttons
        /**
         * @param {?} map
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.initCustomButtons =
            // Initialize custom buttons
            /**
             * @param {?} map
             * @return {?}
             */
            function (map) {
                var _this = this;
                var e_1, _a;
                /** @type {?} */
                var showButtons = this.customButtonsList;
                if (this.onlyCustomButtons && this.onlyCustomButtons.length > 0) {
                    showButtons = this.customButtonsList.filter(( /**
                     * @param {?} a
                     * @return {?}
                     */function (a) { return _this.onlyCustomButtons.includes(a.key); }));
                }
                if (showButtons && showButtons.length > 0) {
                    var _loop_1 = function (button) {
                        /** @type {?} */
                        var controlDiv = document.createElement('div');
                        controlDiv.style.marginRight = '2px';
                        /** @type {?} */
                        var controlUI = document.createElement('div');
                        controlUI.style.backgroundColor = '#fff';
                        controlUI.style.border = '2px solid #fff';
                        controlUI.style.borderRadius = '3px';
                        controlUI.style.boxShadow = '0 2px 6px rgba(0,0,0,.3)';
                        controlUI.style.cursor = 'pointer';
                        controlUI.style.marginTop = '5px';
                        controlUI.style.marginBottom = '5px';
                        controlUI.style.width = '23px';
                        controlUI.style.height = '20px';
                        controlUI.style.textAlign = 'center';
                        controlUI.title = button.label;
                        controlDiv.appendChild(controlUI);
                        /** @type {?} */
                        var controlText = document.createElement('div');
                        controlText.style.color = 'rgb(25,25,25)';
                        controlText.style.fontFamily = 'Roboto,Arial,sans-serif';
                        controlText.style.fontSize = '12px';
                        controlText.style.lineHeight = '20px';
                        controlText.style.paddingLeft = '3px';
                        controlText.style.paddingRight = '3px';
                        controlText.innerHTML = "<span> " + iconValues[button.key] + " </span>";
                        controlUI.appendChild(controlText);
                        controlUI.addEventListener('click', ( /**
                         * @return {?}
                         */function () {
                            this.handleCustomButtonEvent(button.key);
                        }).bind(this_1));
                        map.controls[google.maps.ControlPosition[this_1.position]].push(controlDiv);
                    };
                    var this_1 = this;
                    try {
                        for (var showButtons_1 = __values(showButtons), showButtons_1_1 = showButtons_1.next(); !showButtons_1_1.done; showButtons_1_1 = showButtons_1.next()) {
                            var button = showButtons_1_1.value;
                            _loop_1(button);
                        }
                    }
                    catch (e_1_1) {
                        e_1 = { error: e_1_1 };
                    }
                    finally {
                        try {
                            if (showButtons_1_1 && !showButtons_1_1.done && (_a = showButtons_1.return))
                                _a.call(showButtons_1);
                        }
                        finally {
                            if (e_1)
                                throw e_1.error;
                        }
                    }
                }
                else {
                    this.showError('please check onlyCustomButtons property', this.onlyCustomButtons);
                }
            };
        // Handles custom button events
        // Handles custom button events
        /**
         * @param {?} button
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.handleCustomButtonEvent =
            // Handles custom button events
            /**
             * @param {?} button
             * @return {?}
             */
            function (button) {
                switch (button) {
                    case 'undo': {
                        this.undoShape();
                        break;
                    }
                    case 'clear': {
                        this.deleteAllShape();
                        break;
                    }
                    case 'delete': {
                        this.deleteSelectedShape();
                        break;
                    }
                    case 'save': {
                        this.saveSelectedShape();
                        break;
                    }
                    case 'saveAll': {
                        this.saveAllShape();
                        break;
                    }
                }
            };
        // set selected shape on map
        // set selected shape on map
        /**
         * @param {?} shape
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.setSelection =
            // set selected shape on map
            /**
             * @param {?} shape
             * @return {?}
             */
            function (shape) {
                this.clearSelection();
                this.selectedShape = shape;
                shape.setEditable(true);
                if (this.googleMapObjects) {
                    this.overlaySelected.emit(shape); // output overlay selected
                }
                else {
                    /** @type {?} */
                    var temp = this.getOverlayType(shape);
                    this.overlaySelected.emit(temp);
                }
            };
        // clears selected shape on map
        // clears selected shape on map
        /**
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.clearSelection =
            // clears selected shape on map
            /**
             * @return {?}
             */
            function () {
                if (this.selectedShape) {
                    this.selectedShape.setEditable(false);
                    this.selectedShape = null;
                }
            };
        // deletes selected shape
        // deletes selected shape
        /**
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.deleteSelectedShape =
            // deletes selected shape
            /**
             * @return {?}
             */
            function () {
                if (this.selectedShape) {
                    this.selectedShape.setMap(null);
                }
            };
        // outputs the selected shape
        // outputs the selected shape
        /**
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.saveSelectedShape =
            // outputs the selected shape
            /**
             * @return {?}
             */
            function () {
                if (this.googleMapObjects) {
                    this.saveSeleted.emit(this.selectedShape); // output overlay selected
                }
                else {
                    if (this.selectedShape) {
                        /** @type {?} */
                        var shape = this.getOverlayType(this.selectedShape);
                        this.saveSeleted.emit(shape);
                    }
                }
            };
        // deletes all the shape on map
        // deletes all the shape on map
        /**
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.deleteAllShape =
            // deletes all the shape on map
            /**
             * @return {?}
             */
            function () {
                // tslint:disable-next-line:prefer-for-of
                for (var i = 0; i < this.allOverlays.length; i++) {
                    this.allOverlays[i].overlay.setMap(null);
                }
                this.allOverlays = [];
            };
        // undo the last action
        // undo the last action
        /**
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.undoShape =
            // undo the last action
            /**
             * @return {?}
             */
            function () {
                if (this.allOverlays !== undefined && this.allOverlays !== null && this.allOverlays.length >= 1) {
                    this.allOverlays[this.allOverlays.length - 1].overlay.setMap(null);
                    this.allOverlays.pop();
                }
            };
        // outputs all shapes
        // outputs all shapes
        /**
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.saveAllShape =
            // outputs all shapes
            /**
             * @return {?}
             */
            function () {
                var e_2, _a;
                if (this.allOverlays && this.allOverlays.length > 0) {
                    if (this.googleMapObjects) {
                        this.saveAll.emit(this.allOverlays);
                    }
                    else {
                        /** @type {?} */
                        var shapes = [];
                        try {
                            for (var _b = __values(this.allOverlays), _c = _b.next(); !_c.done; _c = _b.next()) {
                                var overLay = _c.value;
                                if (overLay) {
                                    /** @type {?} */
                                    var shape = this.getOverlayType(overLay);
                                    if (shape) {
                                        shapes.push(shape);
                                    }
                                }
                            }
                        }
                        catch (e_2_1) {
                            e_2 = { error: e_2_1 };
                        }
                        finally {
                            try {
                                if (_c && !_c.done && (_a = _b.return))
                                    _a.call(_b);
                            }
                            finally {
                                if (e_2)
                                    throw e_2.error;
                            }
                        }
                        this.saveAll.emit(shapes);
                    }
                }
            };
        // returns an shape object
        // returns an shape object
        /**
         * @private
         * @param {?} object
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.getOverlayType =
            // returns an shape object
            /**
             * @private
             * @param {?} object
             * @return {?}
             */
            function (object) {
                var e_3, _a;
                /** @type {?} */
                var shape = null;
                /** @type {?} */
                var overlay = null;
                if (object.map) {
                    overlay = object;
                }
                else if (object.overlay.map) {
                    overlay = object.overlay;
                }
                else {
                    this.showError('Error while reading overlay, try googleMapObjects = true', object);
                    return;
                }
                if (object.type === 'circle') {
                    /** @type {?} */
                    var center = { lat: overlay.center.lat(), lng: overlay.center.lng() };
                    /** @type {?} */
                    var circle = { center: center, radius: overlay.radius };
                    shape = {
                        type: object.type,
                        shape: circle
                    };
                }
                else if (object.type === 'polygon' || object.type === 'polyline') {
                    /** @type {?} */
                    var center = { lat: overlay.map.center.lat(), lng: overlay.map.center.lng() };
                    /** @type {?} */
                    var tempPoints = overlay.latLngs.j[0].j;
                    /** @type {?} */
                    var points = [];
                    try {
                        for (var tempPoints_1 = __values(tempPoints), tempPoints_1_1 = tempPoints_1.next(); !tempPoints_1_1.done; tempPoints_1_1 = tempPoints_1.next()) {
                            var p = tempPoints_1_1.value;
                            /** @type {?} */
                            var point = { lat: p.lat(), lng: p.lng() };
                            points.push(point);
                        }
                    }
                    catch (e_3_1) {
                        e_3 = { error: e_3_1 };
                    }
                    finally {
                        try {
                            if (tempPoints_1_1 && !tempPoints_1_1.done && (_a = tempPoints_1.return))
                                _a.call(tempPoints_1);
                        }
                        finally {
                            if (e_3)
                                throw e_3.error;
                        }
                    }
                    /** @type {?} */
                    var polygon = { center: center, points: points };
                    shape = {
                        type: object.type,
                        shape: polygon
                    };
                }
                else if (object.type === 'rectangle') {
                    /** @type {?} */
                    var center = { lat: overlay.map.center.lat(), lng: overlay.map.center.lng() };
                    // tslint:disable-next-line:no-shadowed-variable
                    /** @type {?} */
                    var bounds = overlay.getBounds();
                    /** @type {?} */
                    var neBound = { lat: bounds.getNorthEast().lat(), lng: bounds.getNorthEast().lng() };
                    /** @type {?} */
                    var swBound = { lat: bounds.getSouthWest().lat(), lng: bounds.getSouthWest().lng() };
                    /** @type {?} */
                    var rectangle = { center: center, northEastBound: neBound, southWestBound: swBound };
                    shape = {
                        type: object.type,
                        shape: rectangle
                    };
                }
                else if (object.type === 'marker') {
                    /** @type {?} */
                    var center = { lat: overlay.map.center.lat(), lng: overlay.map.center.lng() };
                    /** @type {?} */
                    var position = { lat: overlay.position.lat(), lng: overlay.position.lng() };
                    /** @type {?} */
                    var marker = { center: center, position: position };
                    shape = {
                        type: object.type,
                        shape: marker
                    };
                }
                return shape;
            };
        // sets shapes on map
        // sets shapes on map
        /**
         * @private
         * @param {?} shapes
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.loadShapeToMap =
            // sets shapes on map
            /**
             * @private
             * @param {?} shapes
             * @return {?}
             */
            function (shapes) {
                if (shapes && shapes.length > 0) {
                    for (var i = 0; i < shapes.length; i++) {
                        /** @type {?} */
                        var shape = shapes[i];
                        if (shape.type === 'marker') {
                            /** @type {?} */
                            var markerObj = shape.shape;
                            /** @type {?} */
                            var marker = new google.maps.Marker({
                                position: markerObj.position,
                                map: this.map,
                                icon: this.markerOption.icon,
                                title: this.markerOption.title,
                                animation: google.maps.Animation[this.markerOption.animation],
                                draggable: this.markerOption.draggable
                            });
                            /** @type {?} */
                            var overlay = {
                                type: 'marker',
                                overlay: marker
                            };
                            this.addOverlay(overlay);
                            if (this.isWorldView && i === 0) {
                                /** @type {?} */
                                var bounds = new google.maps.LatLngBounds();
                                /** @type {?} */
                                var latLng = new google.maps.LatLng(markerObj.position.lat, markerObj.position.lng);
                                bounds.extend(latLng);
                                this.map.fitBounds(bounds);
                            }
                        }
                        else if (shape.type === 'circle') {
                            /** @type {?} */
                            var circleObj = shape.shape;
                            /** @type {?} */
                            var circle = new google.maps.Circle({
                                fillColor: this.circleOption.fillColor,
                                fillOpacity: this.circleOption.fillOpacity,
                                strokeColor: this.circleOption.strokeColor,
                                strokeWeight: this.circleOption.strokeWeight,
                                clickable: this.circleOption.clickable,
                                editable: this.circleOption.editable,
                                draggable: this.circleOption.draggable,
                                zIndex: this.circleOption.zIndex,
                                map: this.map,
                                center: circleObj.center,
                                radius: circleObj.radius
                            });
                            /** @type {?} */
                            var overlay = {
                                type: 'circle',
                                overlay: circle
                            };
                            this.addOverlay(overlay);
                            if (this.isWorldView && i === 0) {
                                this.map.fitBounds(circle.getBounds());
                            }
                        }
                        else if (shape.type === 'polygon') {
                            /** @type {?} */
                            var polygonObj = shape.shape;
                            /** @type {?} */
                            var polygon = new google.maps.Polygon({
                                paths: polygonObj.points,
                                fillColor: this.polygonOption.fillColor,
                                fillOpacity: this.polygonOption.fillOpacity,
                                strokeColor: this.polygonOption.strokeColor,
                                strokeWeight: this.polygonOption.strokeWeight,
                                clickable: this.polygonOption.clickable,
                                editable: this.polygonOption.editable,
                                draggable: this.polygonOption.draggable,
                                zIndex: this.polygonOption.zIndex
                            });
                            polygon.setMap(this.map);
                            /** @type {?} */
                            var overlay = {
                                type: 'polygon',
                                overlay: polygon
                            };
                            this.addOverlay(overlay);
                            if (this.isWorldView && i === 0) {
                                /** @type {?} */
                                var bounds = new google.maps.LatLngBounds();
                                // tslint:disable-next-line:prefer-for-of
                                for (var j = 0; j < polygonObj.points.length; j++) {
                                    /** @type {?} */
                                    var latLng = new google.maps.LatLng(polygonObj.points[j].lat, polygonObj.points[j].lng);
                                    bounds.extend(latLng);
                                }
                                this.map.fitBounds(bounds);
                            }
                        }
                        else if (shape.type === 'rectangle') {
                            /** @type {?} */
                            var rectangleObj = shape.shape;
                            /** @type {?} */
                            var rectangle = new google.maps.Rectangle({
                                fillColor: this.rectangleOption.fillColor,
                                fillOpacity: this.rectangleOption.fillOpacity,
                                strokeColor: this.rectangleOption.strokeColor,
                                strokeWeight: this.rectangleOption.strokeWeight,
                                clickable: this.rectangleOption.clickable,
                                editable: this.rectangleOption.editable,
                                draggable: this.rectangleOption.draggable,
                                zIndex: this.rectangleOption.zIndex,
                                map: this.map,
                                bounds: {
                                    north: rectangleObj.northEastBound.lat,
                                    south: rectangleObj.southWestBound.lat,
                                    east: rectangleObj.northEastBound.lng,
                                    west: rectangleObj.southWestBound.lng
                                }
                            });
                            /** @type {?} */
                            var overlay = {
                                type: 'rectangle',
                                overlay: rectangle
                            };
                            this.addOverlay(overlay);
                            if (this.isWorldView && i === 0) {
                                this.map.fitBounds(rectangle.getBounds());
                            }
                        }
                        else if (shape.type === 'polyline') {
                            /** @type {?} */
                            var polygonObj = shape.shape;
                            /** @type {?} */
                            var polygon = new google.maps.Polygon({
                                paths: polygonObj.points,
                                fillColor: this.polygonOption.fillColor,
                                fillOpacity: this.polygonOption.fillOpacity,
                                strokeColor: this.polygonOption.strokeColor,
                                strokeWeight: this.polygonOption.strokeWeight,
                                clickable: this.polygonOption.clickable,
                                editable: this.polygonOption.editable,
                                draggable: this.polygonOption.draggable,
                                zIndex: this.polygonOption.zIndex
                            });
                            polygon.setMap(this.map);
                            /** @type {?} */
                            var overlay = {
                                type: 'polyline',
                                overlay: polygon
                            };
                            this.addOverlay(overlay);
                            if (this.isWorldView && i === 0) {
                                /** @type {?} */
                                var bounds = new google.maps.LatLngBounds();
                                // tslint:disable-next-line:prefer-for-of
                                for (var j = 0; j < polygonObj.points.length; j++) {
                                    /** @type {?} */
                                    var latLng = new google.maps.LatLng(polygonObj.points[j].lat, polygonObj.points[j].lng);
                                    bounds.extend(latLng);
                                }
                                this.map.fitBounds(bounds);
                            }
                        }
                    }
                }
            };
        /**
         * @private
         * @param {?} message
         * @param {?} object
         * @return {?}
         */
        NgxGoogleMapHelperComponent.prototype.showError = /**
         * @private
         * @param {?} message
         * @param {?} object
         * @return {?}
         */
            function (message, object) {
                if (this.showErrors) {
                    if (object) {
                        console.error("ngx-google-map-helper: " + message, object);
                    }
                    else {
                        console.error("ngx-google-map-helper: " + message);
                    }
                }
            };
        NgxGoogleMapHelperComponent.decorators = [
            { type: core.Component, args: [{
                        // tslint:disable-next-line:component-selector
                        selector: 'ngx-google-map-helper',
                        template: "\r\n<div class=\"map-wrapper\" [ngStyle]=\"{'width': mapWidth, 'height': mapHeight}\">\r\n  <div id=\"map\" class=\"google-map\" style=\"height:100%; margin: 0\"></div>\r\n</div>\r\n\r\n  ",
                        styles: [".map-wrapper{position:relative;left:0;bottom:0}"]
                    }] }
        ];
        /** @nocollapse */
        NgxGoogleMapHelperComponent.ctorParameters = function () {
            return [
                { type: core.ElementRef },
                { type: NgxGoogleMapHelperService }
            ];
        };
        NgxGoogleMapHelperComponent.propDecorators = {
            mapHeight: [{ type: core.Input }],
            mapWidth: [{ type: core.Input }],
            zoom: [{ type: core.Input }],
            center: [{ type: core.Input }],
            mapType: [{ type: core.Input }],
            placeMarkerOnClick: [{ type: core.Input }],
            showControl: [{ type: core.Input }],
            position: [{ type: core.Input }],
            showModes: [{ type: core.Input }],
            locationAccess: [{ type: core.Input }],
            commonOption: [{ type: core.Input }],
            circleOption: [{ type: core.Input }],
            polygonOption: [{ type: core.Input }],
            polylineOption: [{ type: core.Input }],
            rectangleOption: [{ type: core.Input }],
            markerOption: [{ type: core.Input }],
            customButtons: [{ type: core.Input }],
            onlyCustomButtons: [{ type: core.Input }],
            googleMapObjects: [{ type: core.Input }],
            showErrors: [{ type: core.Input }],
            shapes: [{ type: core.Input }],
            mapClicked: [{ type: core.Output }],
            overlayCompleted: [{ type: core.Output }],
            overlaySelected: [{ type: core.Output }],
            saveSeleted: [{ type: core.Output }],
            saveAll: [{ type: core.Output }]
        };
        return NgxGoogleMapHelperComponent;
    }());

    /**
     * @fileoverview added by tsickle
     * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
     */
    var NgxGoogleMapHelperModule = /** @class */ (function () {
        function NgxGoogleMapHelperModule() {
        }
        /**
         * @param {?} config
         * @return {?}
         */
        NgxGoogleMapHelperModule.forRoot = /**
         * @param {?} config
         * @return {?}
         */
            function (config) {
                return {
                    ngModule: NgxGoogleMapHelperModule,
                    providers: [
                        { provide: MapConfig, useValue: config }
                    ]
                };
            };
        NgxGoogleMapHelperModule.decorators = [
            { type: core.NgModule, args: [{
                        imports: [common.CommonModule],
                        declarations: [NgxGoogleMapHelperComponent],
                        exports: [NgxGoogleMapHelperComponent],
                        providers: [NgxGoogleMapHelperService]
                    },] }
        ];
        return NgxGoogleMapHelperModule;
    }());

    /**
     * @fileoverview added by tsickle
     * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
     */

    /**
     * @fileoverview added by tsickle
     * @suppress {checkTypes,extraRequire,missingOverride,missingReturn,unusedPrivateMembers,uselessCode} checked by tsc
     */

    exports.NgxGoogleMapHelperService = NgxGoogleMapHelperService;
    exports.NgxGoogleMapHelperComponent = NgxGoogleMapHelperComponent;
    exports.NgxGoogleMapHelperModule = NgxGoogleMapHelperModule;
    exports.ɵa = MapConfig;

    Object.defineProperty(exports, '__esModule', { value: true });

})));

//# sourceMappingURL=ngx-google-map-helper.umd.js.map